# No API keys or secrets are required to run this application.
# Data is fetched from Yahoo Finance (yfinance) which provides
# free access without authentication.

# Optional: override Streamlit server settings
# STREAMLIT_SERVER_PORT=8501
# STREAMLIT_SERVER_ADDRESS=localhost
